import React, { Component } from "react";
import { Button } from "react-bootstrap";

class Arm extends Component {
  state = {};
  render() {
    return <Button variant="primary">Arm</Button>;
  }
}

export default Arm;
