import React, { Component } from "react";
import { Button } from "react-bootstrap";

class Takeoff extends Component {
  state = {};
  render() {
    return <Button variant="primary">Takeoff</Button>;
  }
}

export default Takeoff;
