import React, { Component } from "react";
import { Button } from "react-bootstrap";

class Kill extends Component {
  state = {};
  render() {
    return <Button variant="danger">Kill</Button>;
  }
}

export default Kill;
